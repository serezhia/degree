import 'package:degree_app/auth/auth.dart';

class GetUrl extends StatelessWidget {
  const GetUrl({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Get Url'),
      ),
      body: const Center(
        child: Text('Get Url'),
      ),
    );
  }
}
